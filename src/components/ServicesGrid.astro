---
const BASE_URL = import.meta.env.BASE_URL;

const items = [
  // Si luego creas /servicios/gigantografias, cambia el href
  { icon: "bx bxs-image", title: "Gigantografías", desc: "Lona 13/18 oz, microperforado, vinil + laminado UV.", href: "#contacto" },

  // ✅ Probando esta página real
  { icon: "bx bxs-bulb", title: "Letreros 3D / Luminosos", desc: "Acrílico, MDF, PVC y luminarias LED.",  href: "servicios/letrero-luminoso" },

  // Si luego creas /servicios/corte-laser, cámbialo aquí
  { icon: "bx bxs-cut", title: "Corte Láser / Router", desc: "Precisión para letras y paneles.", href: "#contacto" },

  // Si luego creas /servicios/vinil-decoracion, cámbialo aquí
  { icon: "bx bxs-paint", title: "Vinil y Decoración", desc: "Rotulado de vehículos, paredes y vidrios.", href: "#contacto" },

  // ✅ Página real ya existente
  { icon: "bx bxs-wrench", title: "Instalación", desc: "Montaje seguro y garantía en toda Arequipa.", href: `${BASE_URL}servicios/instalacion` },
];
---

<section id="productos" class="py-12 md:py-16">
  <div class="container">
    <h2 class="text-3xl md:text-4xl font-extrabold text-center text-primary">
      Nuestros <span class="text-blue-600">Servicios</span>
    </h2>
    <p class="mt-2 text-center text-gray-600">
      Todo lo que necesitas en publicidad e impresión digital.
    </p>

    <div class="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {items.map((it) => (
        <div class="rounded-2xl border p-6 shadow-sm hover:shadow-md transition">
          <div class="text-3xl text-blue-600"><i class={it.icon}></i></div>
          <h3 class="mt-3 text-xl font-bold">{it.title}</h3>
          <p class="mt-1 text-gray-600">{it.desc}</p>
          <a href={it.href} class="mt-4 inline-block text-blue-600 font-semibold hover:underline">
            {it.href.includes('servicios/') ? 'Ver más →' : 'Cotizar →'}
          </a>
        </div>
      ))}
    </div>
  </div>
</section>
